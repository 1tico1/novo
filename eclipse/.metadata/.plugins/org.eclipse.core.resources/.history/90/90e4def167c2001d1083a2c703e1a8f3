package br.com.fiap;

import javax.swing.JOptionPane;

public class ExercicioNovoJOption {

	public static void main(String[] args) {
		float altura, base, area;  
		String aux;
		
		try {
			JOptionPane.showInputDialog("Informe a base do retangulo: ");
			base = Integer.parseInt(aux);
			JOptionPane.showInputDialog("Informe a altura do retangulo: ");
			altura = Integer.parseInt(aux);
			area = Integer.parseInt(aux);
			JOptionPane.showMessageDialog(null, "Valor da area: " + area(base * altura));
			
		} catch (Exception e) {
			JOptionPane.showMessageDialog(null, "Resposta errada");
			
		}

	}

}
